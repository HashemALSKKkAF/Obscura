# Web framework (the app is Flask; serves the SPA + JSON API)
flask

# HTTP, Tor SOCKS, scraping, and JS-rendered crawling
requests
pysocks
bs4
selenium

# LLM providers — LangChain integrations plus the native SDKs that llm.py
# imports directly (openai is a hard import; google-api-core for retry types).
langchain-openai
langchain-anthropic
langchain-ollama
langchain_community
langchain_google_genai
openai
google-api-core

# RAG (0.4.0) — local-only retrieval over crawled content.
#   fastembed: ONNX embeddings computed on-box (no API, no data egress)
#   chromadb:  embedded vector store (persists to disk)
#   numpy:     in-memory cosine store + vector math
fastembed
chromadb
numpy

# Reporting + configuration
reportlab
python-dotenv

# Retained from the original environment (transitive backport pin)
backports.tarfile
